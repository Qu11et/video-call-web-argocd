apiVersion: v1
kind: Service
metadata:
  name: {{ include "video-call-web.fullname" . }} # Tên service chính
  labels:
    {{- include "video-call-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "video-call-web.selectorLabels" . | nindent 4 }}