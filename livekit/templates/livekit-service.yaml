apiVersion: v1
kind: Service
metadata:
  name: livekit
  labels:
    app: livekit
spec:
  type: ClusterIP
  ports:
    - port: 7880
      targetPort: 7880
      protocol: TCP
      name: http
  selector:
    app: livekit